enum SortType {
  byTitle,
  byAuthor,
  byRating,
  byPages,
  byStartDate,
  byFinishDate,
  byPublicationYear,
  byDateAdded,
  byDateModified,
}

enum RatingType {
  bar,
  number,
}

enum Font {
  system,
  montserrat,
  lato,
  sofiaSans,
  poppins,
  raleway,
  nunito,
  playfairDisplay,
  kanit,
  lora,
  quicksand,
  barlow,
  jost,
  inter,
}

enum BookFormat {
  paperback,
  hardcover,
  ebook,
  audiobook,
}

enum BulkEditOption {
  format,
  author,
  delete,
}

enum OLSearchType {
  general,
  title,
  author,
  isbn,
  openlibraryId,
}
